# LibreChat Frontend Phase 1 Interface Matrix

Status: Phase 1 local implementation evidence companion. This document maps the current frontend route surface to existing ai-platform route families and records which standalone pages remain fail-closed until backend contracts exist.

## Rule

Phase 1 may use only current ai-platform public/admin projections or compatibility routes that are already backed by the ai-platform control plane. A page that still depends on legacy product-authority APIs is kept as a Phase 2 unavailable surface and must not call its API client from active `TabContent`.

## Active Phase 1 Surfaces

| Surface | Classification | Frontend entry | Existing route family | Phase 1 behavior |
| --- | --- | --- | --- | --- |
| Auth principal | reuse-current | `frontend/web/src/services/api/auth.ts`, `frontend/web/src/hooks/useAuth.tsx` | `/api/auth/login`, `/api/auth/me`, `/api/auth/refresh`, `/api/auth/permissions` | Uses ai-platform signed principal and normalizes `agent:use`, `artifact:download`, and `admin:status` into effective UI permissions. |
| Chat workspace | reuse-current | `frontend/web/src/components/layout/AppContent/ChatAppContent.tsx`, `frontend/web/src/services/api/session.ts` | `/api/sessions`, `/api/sessions/{id}`, `/api/chat/stream`, `/api/chat/sessions/{id}/stream`, `/api/chat/sessions/{id}/status` | Main chat/session/run loop stays active. Unsupported session actions need separate button-level hardening if exposed. |
| Agent/model selection in chat | reuse-current | `frontend/web/src/services/api/agent.ts`, `frontend/web/src/services/api/modelPublic.ts` | `/api/agents`, `/api/agent/models/available`, `/api/agent/models/providers/list` | Chat-bound read selectors can use current compat/public projections. Composer capability selection uses public agent IDs and `/api/chat/stream`; ordinary users do not see or submit raw skill IDs as authority. |
| Profile default agent preference | reuse-current | `frontend/web/src/components/profile/tabs/ProfilePreferencesTab.tsx`, `frontend/web/src/hooks/useAuth.tsx` | Read choices from `/api/agents`; persist preference through `/api/auth/profile/metadata` | Users can pick a default chat agent from the public capability projection. Phase 1 does not call `/api/agent/config/*`; role/global agent configuration remains Phase 2. |
| Runtime feature flags and model context | reuse-current | `frontend/web/src/contexts/SettingsContext.tsx` | `/api/agent/models/available`, `/api/auth/profile`, `/api/auth/profile/metadata` | The global app provider no longer calls legacy `/api/settings/*`. Chat-bound skills and memory stay enabled by local Phase 1 defaults and are then constrained by permissions and backed projections; model catalog and pinned models use current public/profile routes. Settings writes fail closed until a backend settings projection exists. |
| Sidebar session list | reuse-current | `frontend/web/src/components/panels/SessionSidebar.tsx`, `frontend/web/src/hooks/useSession.ts`, `frontend/web/src/services/api/session.ts` | `/api/sessions` | The sidebar keeps current session listing, unread reset, search, select-session, and new-session entrypoints. Project/favorite organization is not rendered in Phase 1 because the current backend compatibility route does not provide authoritative `project_id` or `favorites_only` filtering. |
| Upload in chat | reuse-current | `frontend/web/src/hooks/useFileUpload.ts`, `frontend/web/src/components/chat/FileUploadButton.tsx` | `/api/upload/config`, `/api/upload/check`, `/api/upload/file` | Document upload remains available when the normalized principal grants upload permissions. Standalone file library remains Phase 2. |
| Run events, artifacts, playback | reuse-current | `frontend/web/src/components/layout/AppContent/RunPlaybackPanel.tsx`, chat message artifact components | `/api/sessions/{id}/events`, `/api/sessions/{id}/runs`, `/api/ai/runs/*`, `/api/ai/artifacts/*` | Run playback, visible events, artifact preview, and download stay in the chat/run workflow. |
| Tool permission cards | reuse-current | `frontend/web/src/services/api/toolPermission.ts`, chat permission cards | `/api/ai/runs/{run_id}/tool-permissions/*` | Existing permission request/decision flow stays active. MCP server lifecycle remains Phase 2. |
| Memory | reuse-current | `frontend/web/src/components/panels/MemoryPanel/index.tsx`, `frontend/web/src/services/api/memory.ts` | `/api/ai/memory/policy`, `/api/ai/memory/records`, `/api/ai/admin/memory/*` | Memory page stays enabled when memory feature and chat/session permissions allow it. Admin memory projection is gated by normalized effective permissions (`admin:status` or `settings:manage`) instead of role names. |
| Admin Runtime | remap-current | `frontend/web/src/components/panels/AdminRuntimePanel.tsx`, `frontend/web/src/services/api/adminRuntime.ts` | `/api/ai/admin/runtime/overview`, `/api/ai/health` | `/settings` is remapped to a read-only Admin Runtime panel for users with `admin:status` or `settings:manage`. Legacy settings CRUD is not active. |
| Skills governance | remap-current | `frontend/web/src/services/api/phase1Projection.ts`, `frontend/web/src/components/panels/phase1ProjectionPanels.tsx` | Default read: `/api/agents`; explicit admin action: `POST /api/ai/admin/skills/sync-builtin` | `/skills` is remapped to an admin-gated Skills governance view. Page load is read-only and shows current public runtime capabilities from `/api/agents`; built-in Skill sync is available only as an explicit admin button. It does not call legacy `/api/skills/*` or `/api/github/*`. Department marketplace, governed Skill list browsing, and public skill catalog remain Phase 2 until backend projections exist. |
| Tool policy inventory | remap-current | `frontend/web/src/services/api/phase1Projection.ts`, `frontend/web/src/components/panels/phase1ProjectionPanels.tsx` | `/api/ai/admin/tool-policies` | `/mcp` is remapped to current admin tool policy inventory. It does not call legacy `/api/mcp/*` or MCP server lifecycle routes. |
| Agent app governance | remap-current | `frontend/web/src/services/api/phase1Projection.ts`, `frontend/web/src/components/panels/phase1ProjectionPanels.tsx` | `/api/ai/agent-apps`, `/api/agents` | `/agents` is `agent:admin` gated and shows admin agent-app projections plus the same public capability projection used by chat. Ordinary users consume `/api/agents` only inside the chat composer. It does not call legacy `/api/agent/config/*`. |
| Model catalog | remap-current | `frontend/web/src/services/api/phase1Projection.ts`, `frontend/web/src/services/api/modelPublic.ts` | `/api/agent/models/available`, `/api/agent/models/providers/list` | `/models` shows read-only model availability and providers. Model CRUD, reorder, provider policy, and import remain Phase 2. |
| Active notifications | remap-current | `frontend/web/src/services/api/phase1Projection.ts`, `frontend/web/src/components/panels/phase1ProjectionPanels.tsx`, `frontend/web/src/components/layout/AppContent/Header.tsx`, `frontend/web/src/components/notification/NotificationDialog.tsx` | `GET /api/notifications/active` | `/notifications`, the header count, and notification dialog/banner use the active-notification read projection only, accepting either the current `{ notifications: [...] }` envelope or the legacy array shape. Notification dismiss and CRUD remain Phase 2. |

## Phase 2 Unavailable Standalone Pages

| Tab/page | Current frontend dependency | Missing or insufficient backend contract | Phase 2 trigger |
| --- | --- | --- | --- |
| `marketplace` | `frontend/web/src/services/api/marketplace.ts` | Uses `/api/marketplace/*`; no department marketplace backend contract. | Department marketplace install/enable/disable/audit APIs. |
| `users` | `frontend/web/src/services/api/user.ts` | Uses `/api/users/*`; no company user admin projection. | Company user lifecycle and department assignment APIs. |
| `roles` | `frontend/web/src/services/api/role.ts` | Uses `/api/roles/*`; compat list is an empty read shell and lacks management semantics. | Role/permission management APIs tied to ai-platform principal authority. |
| MCP server lifecycle | `frontend/web/src/services/api/mcp.ts` | Uses `/api/mcp/*` and `/api/admin/mcp/*`; Phase 1 only remaps tool policy inventory, not server CRUD/import/export/promotion. | MCP server inventory, lifecycle, import/export, department policy assignment, and user-managed server APIs. |
| `feedback` | `frontend/web/src/services/api/feedback.ts` | Uses `/api/feedback/*`; no feedback projection contract. | Feedback submission/reporting APIs. |
| `channels` | channel panel/API family | No channel admin routes in current backend. | Tenant channel configuration APIs. |
| Agent configuration writes | `frontend/web/src/services/api/agent_config.ts` | Uses `/api/agent/config/*`; Phase 1 only remaps `/api/ai/agent-apps`, `/api/agents`, and user metadata-backed default-agent preference. | Agent configuration/global/role/preference APIs. |
| Model administration writes | `frontend/web/src/services/api/model.ts` | Full `/api/agent/models/*` CRUD/reorder/import is not backed; only safe availability routes exist. | Model admin CRUD, provider policy, and reorder APIs. |
| `files` | `frontend/web/src/services/api/revealedFile.ts` | Uses `/api/files/revealed*`; current artifact APIs do not provide a file-library list projection. | File library projection over authorized artifacts. |
| `persona` | `frontend/web/src/services/api/personaPreset.ts` | Uses `/api/persona-presets/*`; no backend projection. | Persona preset browse/governance APIs. |
| Notification dismiss/admin writes | `frontend/web/src/services/api/notification.ts` | Admin CRUD uses `/api/notifications/admin` and dismiss uses `/api/notifications/{id}/dismiss`; Phase 1 only remaps active read projection. | Notification dismiss and admin CRUD APIs. |
| legacy settings CRUD | `frontend/web/src/services/api/settings.ts` | `/api/settings/*` has only an empty compat shell and cannot support settings management. | Settings public/admin projection and write governance APIs. |
| Public shared sessions | `frontend/web/src/components/share/SharedPage.tsx`, `frontend/web/src/services/api/share.ts` | Uses `/api/share/public/{shareId}` and still depends on legacy share authority. Phase 1 keeps `/shared/:shareId` public but renders a fail-closed unavailable page with no API call. | ai-platform share projection, public access policy, revocation, and audit APIs. |
| Sidebar project/session organization | `frontend/web/src/services/api/project.ts`, `frontend/web/src/services/api/session.ts`, `frontend/web/src/components/sidebar/ProjectItem.tsx`, `frontend/web/src/components/sidebar/ProjectMenu.tsx`, `frontend/web/src/components/sidebar/SessionMenu.tsx` | Current backend compatibility routes do not provide authoritative project/favorite filtered session lists. Phase 1 keeps the ordinary session list active and does not render project/favorite grouping, project create/update/delete, project icon/name edits, session move-to-project, drag/drop project moves, favorite toggles, or sidebar share actions. | Governed project/workspace management APIs with tenant/RBAC policy, audit semantics, and explicit session filter contracts. |

## Phase 1 Guardrails

- `frontend/web/src/components/layout/AppContent/TabContent.tsx` must not import backend-missing management panels.
- `frontend/web/src/components/layout/AppContent/phase1SurfacePolicy.ts` is the shared authority for route permissions and navigation visibility.
- `frontend/web/src/auth/aiPlatformPermissions.ts` is the shared authority for frontend effective permissions derived from backend principal strings.
- Active sidebar session loading must use the ordinary `/api/sessions` list without Phase 2 `project_id` or `favorites_only` filters until backend filter semantics are authoritative.
- `admin:status` grants Admin Runtime, tool-policy inventory, and active-notification visibility only; it does not grant settings CRUD, user/role admin, MCP server lifecycle, or model admin writes.
- `agent:use` supports the chat loop and chat-bound selectors. It does not expose standalone management pages.
- `/skills`, `/mcp`, `/agents`, `/models`, and `/notifications` use `frontend/web/src/services/api/phase1Projection.ts`; they must not import legacy management panels or legacy product-authority API clients.
- The global app shell must not import `useSettings` or call `/api/settings/*`; legacy settings CRUD remains dormant Phase 2 work.
- `/shared/:shareId` must stay fail-closed in Phase 1 and must not import `frontend/web/src/components/share/SharedPage.tsx` or call `/api/share/public/*` until ai-platform owns the share projection.
